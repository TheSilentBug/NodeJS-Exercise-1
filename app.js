import { add, subtract, multiply, divide, power } from "./math/index.js";

const a = 10;
const b = 5;

console.log("Add:", add(a, b));         // 15
console.log("Subtract:", subtract(a, b)); // 5
console.log("Multiply:", multiply(a, b)); // 50
console.log("Divide:", divide(a, b));     // 2
console.log("Power:", power(a, b));       // 100000
